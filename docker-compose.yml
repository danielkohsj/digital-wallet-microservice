version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: wallet-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: walletdb